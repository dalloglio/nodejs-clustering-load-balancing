{
  "scripts": {
    "app": "ts-node-dev --respawn --transpile-only src/app.ts",
    "app:clustered": "ts-node-dev --respawn --transpile-only src/app-clustered.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
